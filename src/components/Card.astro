---
import { date } from "astro:schema";

const { data } = Astro.props;
---

<!-- <div class="my-5 lg:mx-5">
    <img class="w-[80vw] lg:w-[30vw]" src={data.image} alt={data.title} />
</div>
<style>
    img {
        border-radius: 20px;
    }
</style> -->

<div class="hidden duration-900 ease-in" data-carousel-item>
            <img src={data.image} class="absolute block w-[80vw] lg:w-[30vw] -translate-x-1/2 -translate-y-1/2 top-1/2 left-1/2" alt={data.title}>
        </div>